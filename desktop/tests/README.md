# Tests Desktop
#
# Phase 3+ :
# - desktop/tests/ : tests unitaires discovery, config, ApiClient
# - tests d'intégration LAN (1 serveur + N clients)
#
# Lancer tests client :
#   cd desktop/client && mvn test
