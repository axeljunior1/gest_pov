# Backend (Maven / Spring Boot)
backend/target/
backend/uploads/
*.class
*.jar
*.war
hs_err_pid*

# Frontend (Vite / React)
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Logs & temp
*.log
*.tmp
*.swp

# IDE / éditeur
.idea/
.vscode/
*.iml
.cursor/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Secrets
.env
.env.*
!.env.example

# Archives Docker livraison client
images/*.tar

# Données licence locale (installation + .lic client)
gest-pov-data/
*.lic

# Backups client (scripts/client-backup.sh)
backups/

# Générateur éditeur (clé privée jamais versionnée)
gest-pov-licgen/
gest-pov-licgen/keys/private_key.pem
gest-pov-licgen/*.lic

# Ancien générateur local (licen/) — clés et deps
licen/
licen/private_key.pem
licen/node_modules/
licen/*.lic

# Clés privées RSA (sauf paire de test CI — voir docs/license-production.md)
**/private_key.pem
!backend/src/test/resources/keys/test_private_key.pem

# Tests / couverture
coverage/
.nyc_output/

# Desktop (Maven / config locale)
desktop/client/target/
desktop/client/config/client.properties
desktop/server-package/build/
desktop/server-package/vendor/*.zip
desktop/server-package/vendor/*.exe
desktop/server-package/vendor/pgsql-extract/
desktop/client-package/build/
