# =============================================================================
# Gest_POV — variables Docker (copier vers .env avant déploiement compose)
# cp .env.example .env
# =============================================================================

# PostgreSQL (service db)
POSTGRES_DB=erp_products
POSTGRES_USER=erp_user
POSTGRES_PASSWORD=change-me-strong-password

# Spring Boot — hôte = nom du service Docker "postgres"
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/erp_products
SPRING_DATASOURCE_USERNAME=erp_user
SPRING_DATASOURCE_PASSWORD=change-me-strong-password

# JWT production (min. 32 caractères)
APP_JWT_SECRET=change-me-jwt-secret-min-256-bits-long-key!!

# Ports exposés sur la machine hôte
FRONTEND_PORT=80
BACKEND_PORT=8080
POSTGRES_PORT=5432
APP_PORT=80

# Images pré-buildées (deploy/compose.images.yml)
BACKEND_IMAGE=monapp-backend:1.0.0
FRONTEND_IMAGE=monapp-frontend:1.0.0
